FROM bluenviron/mediamtx:latest

# La imatge oficial ja porta el binari /mediamtx; només hi afegim la
# configuració pròpia del projecte (autenticació contra el backend, etc.).
COPY mediamtx.yml /mediamtx.yml
